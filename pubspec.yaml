name: pengaduan_dp3a
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Sudah benar
  flutter_localizations: 
    sdk: flutter 

  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0
  # --- PERBAIKAN UTAMA: intl di-upgrade ke versi terbaru (0.20.2 atau lebih tinggi)
  intl: ^0.20.2 
  flutter_svg: ^2.0.9
  hexcolor: ^3.0.1
  
  # Dependencies Firebase
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  
  shared_preferences: ^2.2.2
  webview_flutter: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/maps/maps.html
    - assets/images/logo2.png

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo_aplikasi.jpg"
  min_sdk_android: 21
  remove_alpha_ios: true